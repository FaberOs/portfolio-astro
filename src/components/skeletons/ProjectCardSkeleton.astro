---
/**
 * Skeleton fallback for a single ProjectCard.
 * Replicates the card border / padding / layout with pulse placeholders.
 */
---

<div
  class="flex flex-col justify-between rounded-lg border border-light-300 dark:border-dark-400
    bg-light-50 dark:bg-dark-600 p-5"
>
  <div>
    <!-- Repo icon + title -->
    <div class="flex items-center gap-2 mb-3">
      <div class="h-4 w-4 rounded bg-light-200 dark:bg-dark-500 animate-pulse shrink-0"></div>
      <div class="h-4 w-32 rounded-md bg-light-200 dark:bg-dark-500 animate-pulse"></div>
    </div>

    <!-- Description (2 lines) -->
    <div class="space-y-1.5 min-h-[2rem]">
      <div class="h-3 w-full rounded bg-light-200 dark:bg-dark-500 animate-pulse"></div>
      <div class="h-3 w-2/3 rounded bg-light-200 dark:bg-dark-500 animate-pulse"></div>
    </div>
  </div>

  <!-- Footer: language + year -->
  <div class="flex items-center justify-between mt-4 pt-3 border-t border-light-300 dark:border-dark-400">
    <div class="flex items-center gap-2">
      <div class="h-3.5 w-3.5 rounded-full bg-light-200 dark:bg-dark-500 animate-pulse"></div>
      <div class="h-3 w-16 rounded bg-light-200 dark:bg-dark-500 animate-pulse"></div>
    </div>
    <div class="h-3 w-10 rounded bg-light-200 dark:bg-dark-500 animate-pulse"></div>
  </div>
</div>
